最近买了个大屏显示器(23寸)接在MAC上看电影，但是接上看了两天之后发现，妈的没有遥控器，只能坐在书桌边上看。
于是就有了这个玩意。

功能:提供一个WEB界面可以实现远程按“上、下、左、右、空格、启动程序”，暂无高级功能后续希望实现。

实现方案:用户web—>web.py->appleScript模拟按键

使用方式:运行start.sh脚本，之后用浏览器打开host:8000即可

TODO:太多了，我就不列了，暂时满足我的需求而已。
